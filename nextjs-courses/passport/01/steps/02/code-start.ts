import { config } from '@imtbl/sdk';

// TODO: Complete the configuration for Immutable SDK
export const imtblConfig = {
  // Add the required configuration properties here
  // Hint: You need to specify environment, clientId, redirectUri, and other required fields
}; 